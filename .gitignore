# окружение / секреты
.venv/
.env
__pycache__/
*.pyc

# выход экстрактора (предсказания батча)
pred/
_extractor/pred*/
_extractor/*.tmp

# приватные / реальные документы — НИКОГДА не коммитить (PII)
real_test/
uploads/

# Windows-ярлыки / кэш ОС
*.lnk
.DS_Store
Thumbs.db

# ВНИМАНИЕ: витринный датасет (01_../*.pdf/png/json) НЕ игнорируется намеренно —
# это синтетические (фейковые) fixtures для демо точности. Реальные документы сюда не кладём.
